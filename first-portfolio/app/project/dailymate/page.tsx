import { p4Detail } from "../../../data/projectList";
import ProjectPage from "@/components/projects/ProjectPageComponent";

const Page = () => {
  return <ProjectPage project={p4Detail} />;
};

export default Page;
